// Creo un bucle for
for (let i = 1; i <= 10; i++) {
    // Si el número de iteracion es diferente a 5 mostrar una liena de tabla multiplicacion normal
    if (i != 5) console.log(`5 X ${i} = ${5*i}`);
    // Si no es diferente a 5 entonces continuar con el programa.
    else continue;
}